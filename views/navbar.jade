nav.navbar.bg-primary.row
    div.col-xs-12.col-md-9.text-left
        a.navbar-brand(href="/") Awesome Tips
    div.col-xs-12.col-md-3.text-right
        if (!currentUser)
            ul.userBtns
                // REMOVED ID'S USED BY JQUERY ROUTES, CAUSING SOME ROUTE ISSUES
                //li.glyphicon.glyphicon-log-in#login
                //    a(href="/login") Login
                //li.glyphicon.glyphicon-check#signup
                //    a(href="/sign-up") Signup

                li.glyphicon.glyphicon-log-in.auth
                    a(href="/login") Login
                li.glyphicon.glyphicon-check.auth
                    a(href="/sign-up") Signup

        else
            ul.userBtns
                li
                    a.nav-item(href="/user/" + currentUser.username)=currentUser.username
                    p#userId(hidden)=currentUser.id

                li
                    a.logout.nav-item.glyphicon.glyphicon-log-out.auth(href="/logout") Logout
                    //a.logout.nav-item.glyphicon.glyphicon-log-out(href="/user/")#logout Logout
